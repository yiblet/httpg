(test
 (name test_httpg)
 (deps
  (glob_files ../lib/*.mli)
  (glob_files ../lib/base/*.mli)
  (glob_files ../lib/internal/*.mli)
  (glob_files ../lib/internal/http2/*.mli))
 (libraries
  httpg
  httpg_base
  httpg_internal
  httpg_http2
  alcotest
  lwt
  lwt.unix
  str
  base64
  fmt
  uri))
